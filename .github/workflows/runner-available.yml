name: Test windows-aarch64 availability


on:
  schedule:
    - cron: "30 16-23 * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-aarch64

    steps:
      - uses: actions/checkout@v4

      - name: Success
        run: echo Yay!
